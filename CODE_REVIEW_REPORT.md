# STM32 FOC/BLDC 控制固件代码审查报告

审查日期：2026-07-27

审查范围：`App/`、`Bsp/`（FOC控制核心）+ `Core/Src/main.c`、`tim.c`、`stm32f4xx_it.c` 中与控制环相关的时钟/中断配置。已确认当前未提交改动（`pid.c/h`、`foc_task.c`）是在修复 dt 从 1ms→0.5ms 及 DMA 化 I2C 读取的问题，方向正确，已用实测时钟树核对无误。以下是在此基础上发现的其余隐患，按严重程度排列。

---

## 🔴 高危（直接关系到硬件/人身安全）

### 1. 闭环模式下 PWM 占空比缺少限幅，`OpenLoop` 与三个闭环模式处理不一致

`App/foc_task.c:110`、`126`、`148` 中：

```c
PWM_TIM2_Set((uint16_t)(FOC_PWM_PERIOD*SVPWM_M0.tcm1), ...);
```

直接把浮点数强转 `uint16_t`，没有走 `PWM_LimitCompare()`（`Bsp/foc_drv.c:23`）。而 `OpenLoop_Control()`（`App/foc_task.c:195-197`）明确用了 `PWM_LimitCompare` 做 `[0, FOC_PWM_PERIOD]` 限幅——说明作者已经意识到需要限幅，却唯独漏了电流环/速度环/位置环这三条真正会被 PID 输出推向饱和的路径。

**风险**：一旦 `tcm1/2/3` 因为过调制、PID 输出饱和、`Udc` 估计错误等原因跑出 `[0,1]`，`(uint16_t)` 对负浮点数的转换是未定义行为，实测在 Cortex-M 上通常会得到一个远大于 `FOC_PWM_PERIOD(3360)` 的比较值，导致该相 PWM 在计数周期内永远不匹配比较，从而卡在全通或全断——三相桥臂占空比失衡，可能造成直通/过流/器件烧毁。

**建议**：把这三处也统一换成 `PWM_LimitCompare()`，或者在 `PWM_TIM2_Set` 内部统一做限幅，不要依赖调用方各自记得加。

### 2. `Udc` 是写死的常量，没有真实母线电压反馈

`Core/Src/main.c:91` `float Udc = 12.6f;`，全局搜索确认整个工程没有任何 ADC 采样母线电压并回写 `Udc` 的代码。而 `SVPWM_timer_period_set()`（`Bsp/foc_drv.c:134`）里 `K = √3·Ts/Udc` 直接用这个常量。

**风险**：实际电池电压偏离 12.6V（欠压/满充/负载压降）时，SVPWM 调制深度计算系统性偏差；若实际电压更高，会让上面第1条的"越界不限幅"问题更容易触发。

**建议**：至少用一路 ADC 实时采样母线电压做低通滤波后喂给 `Udc`；短期内如果暂不接线，也应该在文档/常量名上标注这是假设值，并配合第1条的限幅兜底。

### 3. 故障处理机制形同虚设：Fault Handler 死循环、无独立看门狗

`Core/Src/stm32f4xx_it.c:123-131`（`HardFault_Handler`）、`BusFault_Handler`、`UsageFault_Handler` 都是 CubeMX 默认模板：进 `while(1){}` 空转，既不关闭 TIM2 的 PWM 输出，也不做任何保护动作。同时全工程未发现 `IWDG`/`WWDG` 初始化。

**风险**：TIM10 中断（FOC 控制环）一旦因为某次异常（比如 I2C 总线卡死引发的连锁问题、除零、栈溢出）触发 HardFault，CPU 卡死在空循环里，但 TIM2 仍在硬件层面按最后一次写入的 CCR 值持续输出 PWM——电机会带着"定格"的占空比一直转/一直堵转，没有任何自动断电或复位机制。

**建议**：
- Fault Handler 里至少强制把 TIM2 三路 PWM 拉到 0%/50%（安全矢量）或直接失能 TIM2 输出；
- 加入独立看门狗 IWDG，主循环/FOC ISR 定期喂狗，异常挂死时能触发硬件复位。

---

## 🟡 中危

### 4. 电流采样的相序映射、分流电阻阻值均为"未经硬件验证的假设"

`Bsp/current_sense.c:17-27` 注释里作者自己写明：ADC 注入通道到物理 A/B/C 相的对应关系"原理图导出文本未能完全确认逐脚对应关系"；`Bsp/inc/current_sense.h:9` 又写明分流电阻"原理图丝印标注1mΩ，与实物不符，以实物5mΩ为准"。这两个假设直接决定了 `CURRENT_ADC_TO_AMP` 换算系数和 Clarke/Park 变换的相位正确性。

**风险**：如果相序猜错，FOC 会在电角度上引入固定相移，轻则效率差/异响，重则形成正反馈（电流环越纠正越跑偏）导致失控加速；分流电阻若实际不是5mΩ，所有电流值、电流环增益、`Diag_CurrentFault` 阈值都整体偏差达数倍。

**建议**：上板后用第一条注释里描述的方法核实（单独给A相通电流，看报告在哪个通道上），并用万用表实测分流电阻，这是能不能安全带载运行的前提条件，不是代码逻辑问题但必须在报告里点出来。

### 5. 共享中断向量的优先级被设置两次，靠初始化顺序"偶然"正确

`TIM1_UP_TIM10_IRQn` 这一个中断向量被设置了两次优先级：
- `Core/Src/tim.c:321-322`（TIM1 的 MspInit 里）设为 `6`
- `Core/Src/tim.c:373-374`（TIM10 的 MspInit 里）设为 `4`，注释写着"higher priority than USART1 for 1ms FOC loop"

当前能正确工作，纯粹是因为 `Core/Src/main.c:283-293` 里 `MX_TIM10_Init()` 恰好排在 `MX_TIM1_Init()` 之后，后设置的 `4` 生效覆盖了前面的 `6`。

**风险**：这是个隐性耦合——一旦用 CubeMX 重新生成代码、或者有人调整了初始化调用顺序，FOC 控制环的中断优先级会被悄悄改回 `6`（低于 USART1 的 `5`），控制环开始被串口中断抢占，且编译不会有任何报错/警告。

**建议**：把 TIM10 的 NVIC 优先级设置移到只在 `TIM10` 分支里做一次，删掉 TIM1 分支里对同一 IRQn 的设置（或反之保留一处、加注释说明"此 IRQn 与 TIM10 共享，勿在别处重复设置"）。

### 6. `SMO.c` 里的锁相环积分器用了未初始化的局部变量（潜伏 bug）

`Bsp/SMO.c:134,137`：

```c
float x , P_Partern , I_Partern ;
...
I_Partern += PLL_Ki*x;   // I_Partern 是自动局部变量，没有 static，也没有初始化
```

每次调用 `PLL_SMO()`，`I_Partern` 都是栈上的垃圾值，`+=` 读取的是未定义的初值，且从不跨调用持久化——积分作用完全失效，等价于随机数。

目前经确认 `SMO()`/`PLL_SMO()`/`Algorithmic.c` 里的滤波器函数都没有被 `foc_task.c` 或 `main.c` 实际调用（工程里当前的无感算法路径是 AS5600 编码器闭环，不是 SMO 观测器），所以暂时是死代码、不影响现在的运行。但如果以后想切回真正的"Sensorless"（工程名字本身暗示了这个方向），这个 bug 会直接让锁相环失效。

**建议**：`I_Partern` 加 `static`（参照同文件里 `Theta_fore_Last`、`We_fore` 的写法）；如果短期内确定不用 SMO 路径，也建议在文件顶部加说明"当前未接入主控制流程，仅保留供后续无感方案参考"，避免以后有人以为它是能用的。

### 7. 系统时钟用内部 HSI（16MHz RC），未用外部晶振

`Core/Src/main.c:381-382` `RCC_OscInitStruct.OscillatorType = RCC_OSCILLATORTYPE_HSI`。整个168MHz主频、TIM10的2kHz控制节拍、UART波特率、I2C时钟全部派生自这颗内部RC振荡器。

**风险**：HSI 精度通常在 ±1% 量级且随温度漂移，比外部晶振（一般几十 ppm）差两个数量级。当前把 `FOC_LOOP_DT_S` 写死为 `0.0005f` 参与 PID 积分/微分计算，隐含假设了这个节拍绝对精确；HSI 漂移会让 Ki/Kd 的实际生效增益、`Mech_RPM` 计算出现同比例的系统性误差，温度变化时控制环表现会跟着漂。

**建议**：如果板子上有外部晶振，切到 HSE；如果没有，至少在文档里注明这个精度限制，不要假设 dt 是"精确"的0.5ms。

---

## 🟢 低危 / 代码质量

- **`AS5600.c` 直接操作 HAL 内部状态机**：`Bsp/AS5600.c:8-37` `AS5600_CancelPendingTransfer()` 直接改写 `hi2c->State/Mode/ErrorCode` 来"取消"上一次DMA传输，而不是完全依赖官方 Abort API。现在能用，但强耦合 HAL 库内部字段，升级 HAL 版本后可能悄悄失效或留下总线残留状态，建议加注释说明这是有意的hack并注明对应的HAL版本。
- **`AS5600_GetVelocity()`（`Bsp/AS5600.c:225-249`）内部用了阻塞 `printf` 调试输出**，且该函数当前无人调用（`angle_proc` 走的是 `MechSpeed_Update`）。是个"看起来能用、其实是坑"的死函数——如果以后有人在 ISR 里调它，`printf`（走UART阻塞发送）会直接把控制环卡死。建议删掉调试 `printf` 或者干脆删掉这个未使用的函数。
- **`main.c` 里混入一行看着像误粘贴的 commit hash**：`Core/Src/main.c:438` `//7ee75574453dedc37120c1ddd948099600afb96d`，业务上没有意义，建议清理。
- **中文注释乱码（GBK/UTF-8 编码不一致）**：`App/Init_file.c:26`、`Core/Src/main.c` 按键状态机附近（约438-468行）等处仍有 `????` 乱码注释，`pid.c` 里这类乱码已经在本次未提交改动中修复。建议统一工程文件编码为 UTF-8（或按 Keil 工程习惯统一为 GBK），并批量清理剩余乱码，纯可读性问题，不影响功能。
- **`Init_file.c` 的 `Init_All()` 是死代码**：调用点在 `Core/Src/main.c:295` 被注释掉，且内容和 `IMU_task.c` 里已执行的初始化重复，建议直接删除该文件或明确标注废弃。

---

## 优先级建议

1. 先补 **#1 PWM限幅** 和 **#3 故障保护/看门狗**——这两条是"固件一旦出问题，电机/驱动板可能被烧"的直接路径，成本低（分别是几行代码 + IWDG初始化）。
2. 上板测试前务必按 **#4** 核实相序和分流电阻实测值，这是所有电流环调参的地基。
3. **#2 Udc 硬编码**如果暂时没有电压采样通道，先在代码里加注释+ TODO，配合 #1 的限幅兜底。
4. **#5、#6、#7** 属于"目前凑巧没炸，但下一次改动可能引爆"的隐患，建议找个时间窗口一并清理。
